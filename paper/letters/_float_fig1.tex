\begin{figure}[!t]
  \centering
  \includegraphics[width=\columnwidth]{figure_1_latency_boxplot.png}
  \caption{Per-trial wire-level latency by pipeline and condition. The mlc-binary panel (zero I\textsuperscript{2}C transactions on the decision path) isolates the kernel/gpiod latency floor; note the per-panel $y$-axis scaling. Box = IQR (Q1--Q3); whiskers = 1.5$\times$IQR; $\triangle$ = mean; $\bullet$ = outlier; y-axes are capped per panel, with outliers above each cap noted in-panel.}
  \label{fig:latency}
\end{figure}