# 2026-05-20

## Training data pipeline + session 1

### Subtask A: imu_logger configurable ODR

Added --odr and --fflush CLI flags to imu_logger.c. Default 208 Hz
(matches training-data-spec). Allowed: 26, 52, 104, 208, 416, 833 Hz.
Time-based progress messages every ~2 sec regardless of ODR.

Verified: clean compile (two pre-existing warnings on read()
return-value remain; not introduced by this change). Sensor configures
at 208 Hz, measured rate ~212 Hz (within chip tolerance per LSM6DSOX
datasheet).

Committed: cb7e873.

### Subtask B: Saleae automation + orchestrator

Installed logic2-automation Python package on Asus desktop. Enabled
automation server in Logic 2 settings. Confirmed API reachable, target
device 6F657C15C3EEE446 (Logic Pro 8) connected.

Wrote code/orchestrator/run_session.py — coordinates Saleae timed
captures with imu_logger + servo_sweep on Jetson via SSH. Per class:
commands servo to center, starts Saleae capture (D0+D2, 12.5 MS/s),
runs imu_logger blocking for duration, copies artifacts back, writes
session.json.

CLI: --session-date YYYY-MM-DD --class {still|motion|both} --duration N
[--btest]

Btest mode forces duration ≤ 30 sec, suffixes session date, and skips
the existing-dir overwrite refusal. Real sessions refuse to overwrite.

Committed: 69997a8.

### imu_logger truncation bug

During btest, last CSV row was truncated (3 fields instead of 4, no
trailing newline). Diagnosed: process killed before stdio buffer
flushed to disk. SIGTERM handler was missing — only SIGINT handled.

First fix attempt added signal(SIGTERM, on_sigint). Still produced
truncation at 30-sec duration but not at 5-sec, suggesting buffer-size
race rather than signal-handling race. Stopped chasing root cause.

Second fix added setvbuf(_IOLBF) for line-buffered output. Every row
flushes on \n. Defends against any abrupt termination (SIGTERM,
SIGKILL, power loss). Cost: one syscall per sample, negligible at
208 Hz on Orin Nano.

Both fixes committed together: 04957ae.

Verified end-to-end: btest with new logger produces clean trailing
newline, 4-field last row, "Stopped." cleanup message appears.

### Subtask C: session runbook

Wrote docs/session-runbook.md with pre-session/execution/post-session
structure. Pre-session: bench verification, PCA9685 init, Logic 2
readiness, sensor sanity, process hygiene. Execution: orchestrator
command + what to watch. Post-session: file integrity checks, Saleae
spot-check, notes.md write, lab notebook entry.

Explicit rules for marking a session unusable: variance ratio < 50×,
truncated rows beyond final row, sweep log < 80% expected transitions,
Saleae anomalies, human disruption.

Committed: 20aa9e7.

### Session 1 captured

Ran first real training data session:
- Date: 2026-05-20 (label)
- Start: 21:14 PST
- Duration: 1200 sec per class
- Both classes captured
- Still: 254,590 samples, X stddev 0.000766 g
- Motion: 254,561 samples, X stddev 0.117 g
- Variance ratio: 153×
- Sweep transitions: 1200 exact

Post-session checks PASS for everything except Saleae spot-check,
deferred to next session day.

### State at end of session

- Repo at 20aa9e7 (origin/main)
- Jetson clone also at 20aa9e7
- Session 1 data in data/training/2026-05-20/ — uncommitted
- Data commit policy not yet decided
- Servo at center position, bench powered on, USB brick connected

### To do next

- Spot-check Saleae .sal files from session 1
- Session 2 (no earlier than 2026-05-21 per spec ≥24h spacing rule)
- Session 3 (no earlier than 2026-05-22)
- Decide data commit policy before sessions accumulate too much

## 2026-05-21 update — session 1 data committed via Git LFS

Set up Git LFS for the repo. Tracking patterns in `.gitattributes`:
- `*.sal` (Saleae binary captures)
- `data/training/**/accel.csv` (IMU sample logs, ~9 MB each)
- `data/raw/**/*.csv` (future measurement campaign data)
- `*.logicdata`, `*.logicdata2` (legacy Saleae format)

Committed session 1 data: ~21 MB across 4 LFS files (2 accel.csv,
2 saleae.sal) and 3 vanilla git files (notes.md, session.json,
sweep.log).

Hit one ignore-rule bug along the way: the LaTeX-build `*.log`
pattern in `.gitignore` was matching `sweep.log` from
`servo_sweep`. The sweep logs are valuable data (ground-truth
label timestamps for PWM transitions; cross-correlatable with
Saleae captures). Added an exception:
`!data/training/**/*.log`. Sweep logs are now tracked alongside
the other session artifacts.

Also fixed stale Saleae probe table in `docs/pin-assignment.md`
(D2 = PCA9685 PWM, not spi1_sck) and updated README status from
Phase A to Phase B.

Commits: 833e33a, 8c995a7, 5a94fb1, 3da77d8, 5b34649.
